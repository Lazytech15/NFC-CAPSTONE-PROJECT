[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Optional form configuration
[forms]
  [forms."student-registration"]
    name = "Student Registration"
